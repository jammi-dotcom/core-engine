name: Core Engine Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Print message
        run: echo "Building core engine..."
      - name: Build core engine
        run: |
          echo "Building core engine..."
          # Add build commands here